import type { BlurStyle, ToastPosition, ToastQueueMode } from './types';

export const DEFAULT_DURATION = 4000;
export const DEFAULT_POSITION: ToastPosition = 'top';
export const DEFAULT_HAPTIC = true;
export const DEFAULT_BLUR_STYLE: BlurStyle = 'systemMaterial';
export const DEFAULT_QUEUE_MODE: ToastQueueMode = 'replace';
